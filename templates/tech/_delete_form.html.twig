<form method="post" action="{{ path('tech_delete', {'id': tech.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tech.id) }}">
    <button class="btn">Delete</button>
</form>
