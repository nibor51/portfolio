{% extends 'base.html.twig' %}

{% block title %}Tech{% endblock %}

{% block body %}
    <h1>Tech</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tech.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tech.name }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ tech.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tech_index') }}">back to list</a>

    <a href="{{ path('tech_edit', {'id': tech.id}) }}">edit</a>

    {{ include('tech/_delete_form.html.twig') }}
{% endblock %}
