{% extends 'base.html.twig' %}

{% block title %}ImageWork{% endblock %}

{% block body %}
    <h1>ImageWork</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_work.id }}</td>
            </tr>
            <tr>
                <th>Alt</th>
                <td>{{ image_work.alt }}</td>
            </tr>
            <tr>
                <th>Src</th>
                <td>{{ image_work.src }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('image_work_index') }}">back to list</a>

    <a href="{{ path('image_work_edit', {'id': image_work.id}) }}">edit</a>

    {{ include('image_work/_delete_form.html.twig') }}
{% endblock %}
